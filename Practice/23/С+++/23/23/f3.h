#ifndef F3_H
#include "f1.h"
#include <iostream>

int sin()
{
    double x, y, rezult = 0;
    int i = 0;
    for (i; i < y; ++i)
        rezult = rezult + (pow(-1, i) * (pow(x, 2 * i + 1)) / (factorial(2 * i + 1)));
   
}
#endif F3_H

