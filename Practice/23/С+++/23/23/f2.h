#ifndef F2_H
#define F2_H
#include "f1.h"
#include <iostream>

double sin(double i, int x) {
	int i, x;
	double rezult = 0;
	for (int i = 0; i < k; i++)
	{
		res += pow(-1, i) * (pow(x, (2 * i + 1)) / factorial(2 * i + 1));
	}
	return rezult;
#endif F2_H
