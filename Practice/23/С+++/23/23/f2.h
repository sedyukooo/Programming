#ifndef F2_H
#include "f1.h"
#include <math.h>
#include <iostream>
#define F2_H

int sin()
{
    int i = 0;
    double x, y, rezult = 0;
    for (i; i < y; i++)
        rezult = rezult + pow(-1, i) * (pow(x, 2 * i + 1) / (factorial(2 * i + 1)));
    return rezult;
}

#endif
